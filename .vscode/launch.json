{
  "configurations": [
    {
      "type": "msedge",
      "request": "launch",
      "name": "Launch Vue App",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "npm: serve"
    }
  ]
}